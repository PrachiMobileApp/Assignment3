<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.neu.madcourse.prachisharma">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="NUMAD17F-PrachiSharma"
        android:roundIcon="@drawable/madimage"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:versionCode="2"
        android:versionName="1.0.2">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AboutActivity"
            android:label="About Me" />
        <activity
            android:name=".DictionaryActivity"
            android:label="Test Dictionary" />
        <activity
            android:name=".NewGameActivity"
            android:label="New Game" />
        <activity
            android:name=".PhaseTwoActivity"
            android:label="Phase:2" />
        <activity
            android:name=".GameOverActivity"
            android:label="SCROGGLE" />
        <activity android:name=".StartingNewGame"
            android:label="SCROGGLE"></activity>
    </application>

</manifest>